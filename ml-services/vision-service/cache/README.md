# cache/
# Reserved for Phase 2/3: precomputed SigLIP text embedding cache.
# Do NOT store any files here until embedding pipeline is implemented.
#
# Expected future structure:
#   cache/embeddings_furniture.npy   — SigLIP text embeddings for furniture
#   cache/embeddings_electronics.npy
#   cache/label_map.json             — concept_id → canonical_label reverse map
#   cache/alias_map.json             — alias → canonical_label lookup
#
# Embeddings are generated by: python scripts/export_embeddings.py
# Regenerate cache whenever taxonomy CSVs change.
